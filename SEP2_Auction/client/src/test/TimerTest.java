/*
package test;

import model.domain.Timer;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class TimerTest
{
  private Timer timer;

  @BeforeEach void setUp()
  {
    timer = new Timer(10, 1);
  }

  @Test void test_Zero_Seconds()
  {
    Timer zeroTimer = new Timer(0, 2);
    zeroTimer.run();
  }

  @Test void test_One_Second()
  {
    Timer oneTimer = new Timer(1, 3);
    oneTimer.run();
  }

  @Test void test_Many_Seconds()
  {
    timer.run();
  }

  @Test void test_Seconds_Boundary()
  {
    Timer maxTimer = new Timer(Long.MAX_VALUE, 4);
    maxTimer.run();
  }
}

*/
